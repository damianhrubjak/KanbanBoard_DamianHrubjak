package sk.uniza.semestralka.entities

/**
 * Enum to define type of task
 *
 */
enum class TaskType {
    UNDEFINED,
    TO_DO,
    DOING,
    DONE
}

/**
 * Enum to define color of task
 *
 */
enum class Color {
    DEFAULT,
    YELLOW,
    GREEN,
    RED,
    BLUE
}

/**
 * Enum to define direction
 *
 */
enum class Direction {
    BACK,
    FORWARD
}

